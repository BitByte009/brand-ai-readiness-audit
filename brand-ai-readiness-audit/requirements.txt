# Minimal, deliberately small. Every entry must justify itself against the
# portability and package-size constraints (R-56, R-59).

requests>=2.31          # HTTP collection
beautifulsoup4>=4.12    # HTML parsing
lxml>=5.0               # parser backend for bs4
jsonschema>=4.21        # report + observation schema enforcement

# Optional capability. Absent => render lens disabled, render-dependent checks
# become coverage gaps (never silent passes). See references/degraded-mode.md.
# playwright>=1.44
